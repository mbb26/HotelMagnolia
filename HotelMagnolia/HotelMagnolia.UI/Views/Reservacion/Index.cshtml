@model IEnumerable<HotelMagnolia.UI.Models.RESERVACION>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FECHA_ENTRADA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FECHA_SALIDA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ESTADO_RESERVACION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_HABITACION1.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.USUARIO.NOMBRE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_ENTRADA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FECHA_SALIDA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ESTADO_RESERVACION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_HABITACION1.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.USUARIO.NOMBRE)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_RESERVACION })
            @Html.ActionLink("Detalles", "Details", new { id=item.ID_RESERVACION })
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID_RESERVACION })
        </td>
    </tr>
}

</table>
