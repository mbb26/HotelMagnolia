@model IEnumerable<HotelMagnolia.UI.Models.PRECIO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TIPO_PRECIO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PRECIO1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_PRECIO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PRECIO1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PRECIO }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PRECIO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PRECIO })
        </td>
    </tr>
}

</table>
