@model IEnumerable<HotelMagnolia.UI.Models.TEST>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TEST1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NUMERO_TEST)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TEST1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NUMERO_TEST)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_TEST }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_TEST }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_TEST })
        </td>
    </tr>
}

</table>
